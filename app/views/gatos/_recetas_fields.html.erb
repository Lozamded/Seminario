<div class="nested-fields">

  <div class="field">
      <% f.label :nombre %><strong>Medicamento</strong>
      <%= f.text_field :nombre%>
  </div>

  <div class="field">
      <%= f.label :cantidad %>
      <%= f.text_field :cantidad%>
  </div><br>

  <div class="field">
      <%= f.label :unidad %>(especificar para la dosis ya sea en ml, comprimidos, cucharadas, etc)<br>
      <%= f.text_field :unidad%>
  </div>

  <div class="field">
      <%= f.label :cada %>
      <%= f.number_field :cada%>[Hrs]
  </div>

  <div class="field">
      <%= f.label :durante %>
      <%= f.number_field :durante%>[Hrs]
      <% f.label :durante_unidad%>
      <%= f.select :durante_unidad, ['Dias','Meses','Años','Permanente']%>
  </div>

  <strong><%= link_to_remove_association "Borrar medicamento", f %></strong><br><br>

</div>
